import cart from './assets/cart.png'

const CartWidget = () => {
    return (
        <div>
            0
        <img src={cart} alt="cart-widget" />
        
        </div>
    )
}

export default CartWidget